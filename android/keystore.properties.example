# Android release signing — copy to keystore.properties and fill in values.
# NEVER commit keystore.properties or your .keystore / .jks file.

storeFile=../release.keystore
storePassword=YOUR_STORE_PASSWORD
keyAlias=gta-sa-toolkit
keyPassword=YOUR_KEY_PASSWORD
